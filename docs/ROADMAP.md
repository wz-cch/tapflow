# 開發列表

> 已規劃但尚未動工的項目。**這裡只放「要做的」** —— 已知無解或不在 app 能修範圍的，開在 [GitHub Issues](https://github.com/wz-cch/tapflow/issues) 並標 `known-issue`。
>
> 里程碑分期見 [SPEC.md](SPEC.md) 第十三節。這份列表是使用者回饋來的，不完全對應 M3 / M4。
>
> **沒有階段釋出計畫** —— 整個開發完才算一個完整版本,所以這裡不排 milestone。

---

## 待議的互動設計

**只記錄,還沒改。** 這些是實際用過之後浮出來的,列在這裡是為了不讓它們被當成刻意的設計。

### `💾` 長按 = 另存新片段

**這是權宜作法,不是認可的設計。** 當時存 / 讀檔畫面還沒做完,長按只是暫時的出口。使用者明確表示不喜歡這個做法。

重點在於**不要拿它當「本專案用長按當變體」的前例** —— 它本身就還沒被討論過。真要動的時候連同存 / 讀檔那組畫面一起想。

### 錄製時記下觸點底下的節點文字

**這是唯一會改變「編輯到底能做到什麼」的方向,比任何一顆按鍵都值錢。**

現在列表上是 `47 ▸ 輕觸 540, 1200`。座標不是意義 —— 它沒告訴你那一下按到了什麼。而一份讀不懂的腳本,再多的編輯功能都是在上面盲目動刀,這是 SPEC §9.11 用來砍掉重排、絕對位置搬移、型別互轉的判準。

做法:錄製時順手讀當下觸點底下那個 `AccessibilityNodeInfo` 的文字,存進步驟裡當標籤。列表就變成 `47 ▸ 輕觸「確認」`。讀視窗內容這個能力 M4 為了 `AwaitTextNode` 已經要做(`NodeFinder.kt`),不是新增的架構。

**還沒驗證,先列出已知的坑:**

- 自繪畫面沒有節點可讀 —— Canvas、遊戲、WebView 的一部分,拿到的會是空的或整片一個節點
- `getRootInActiveWindow()` 在錄製的熱路徑上有成本,而錄製最不能容忍的就是延遲(§10 那條逐手勢補發的時序已經很緊)
- 觸點與節點解析之間有時間差 —— 手指離開時畫面可能已經因為那一下而變了,讀到的會是下一頁的節點
- 空標籤要怎麼退回顯示座標,而且不能讓兩種格式在列表裡看起來像兩種東西

### 畫布上的長按

`👁`、拖曳、單擊選取都有主人,**長按目前空著**。雙擊曾經被考慮拿來開設定頁,後來因為設定頁改成從主選單進而不需要了(SPEC §9.7)。

先留白。沒有為了填而填的理由,真的出現需求再指派。

### 已解決,留紀錄

- **等待的秒數改不了** —— 已補。設定頁多了「等待」那一列(SPEC §9.5)。下限一秒,因為 0 秒是等待與手動暫停的分界,允許打 0 等於讓一個數字偷偷換掉步驟型別
- **步驟型別的互轉** —— 決定不做。手動暫停與秒數等待沒有位置資訊,要變成一個點就是刪掉再錄一個;反方向直接按 `⏸+`。互轉這個功能本身沒有比這兩步更短(SPEC §9.4)

---

## 未來的方向:把存檔拉出去,在瀏覽器裡編輯

**列為未來需求。現階段優先把現有 app 的功能做完整。**

想法是匯出片段 JSON,在一個(另外寫的)網頁編輯器裡改,再匯回來。

**為什麼這條路有結構性的價值:讓手機上編輯難用的每一個限制,都是手機的限制,不是編輯的限制。**

| 手機上的限制 | 瀏覽器裡 |
|---|---|
| overlay 一律 `FLAG_NOT_FOCUSABLE`,不能放文字欄位(SPEC §9.5) | 想打什麼打什麼 |
| 一次只能有一個大視窗(SPEC §3) | 列表 + 詳情 + 預覽同時擺 |
| 100 個標記互相重疊 | 可縮放、可篩選 |
| 沒有多選、沒有範圍操作 | 免費 |
| 靠 `«` `»` 一格一格搬 | 拖曳排序 |

而且 app 這邊要做的範圍很窄 —— 匯出一份有文件、有版號的 JSON,再安全地讀回來。匯出本來就排在 M3(`ClipDetailScreen`),匯出匯入 JSON 排在 M4。編輯器是另一個專案。

### 它給出的一條分工原則

**能不能在手機上做,取決於它需不需要「真實的那個畫面在你手指下面」。**

- **留在手機上** —— 播放停在第 47 步、座標偏了 20px、改完立刻重跑。網頁編輯器**做不到**,它沒有那個 app
- **交給網頁** —— 重排、範圍刪除、批次改延遲、改名、重構

這條原則回頭確認了 SPEC §9.3 / §9.11 砍掉的每一項(只做 ±1 移動、不做絕對位置、不做型別互轉、插入只往後):它們不是能力不足,是**結構性操作不該在懸浮視窗上做**。以後再有類似提案,先問這一句。

### 兩個先記下來的代價

1. **JSON 從私有格式變成對外契約。** 現在唯一的相容性要求是「舊草稿還能載入」;一旦有編輯器讀它,`strokes`、`delayBefore` 這些欄位名就是 API。而 `Clip` **目前沒有版號欄位** —— 走這條路要先加一個明確的 `schemaVersion`,靠模型隱性相容遲早會踩到
2. **匯入要真的驗證,不能假設檔案是善意的。** 最尖的一根是**重複的 id**:選取、拖曳、標記、undo 全部靠 id,兩步同 id 不會當場崩,是之後某個動作才詭異。其他要擋的:不認識的 `type`、缺必填欄位、負的時長、`screen` 遺失(座標沒有它無法縮放)

---

## 5. 支援 Shizuku

**排最後,價值待評估。**

Shizuku 提供一個 ADB 權限等級的行程,可以繞過 `AccessibilityService.dispatchGesture` 直接注入輸入事件。

### 會解決什麼、不會解決什麼

| 目前的問題 | Shizuku |
|---|---|
| [#1](https://github.com/wz-cch/tapflow/issues/1) 手勢注入器卡住(固定 ~1010ms) | ✅ **完全解決** —— 直接繞過 `dispatchGesture` |
| app 偵測無障礙服務是否啟用 | ⚠️ 部分 —— 注入可以不靠 a11y,但 overlay 要從 `TYPE_ACCESSIBILITY_OVERLAY` 退回 `SYSTEM_ALERT_WINDOW` |
| [#2](https://github.com/wz-cch/tapflow/issues/2) `filterTouchesWhenObscured` | ❌ **完全沒解** |

**#2 沒解的原因很重要:** `FLAG_WINDOW_IS_OBSCURED` 是關於**畫面上有沒有蓋著視窗**,跟手勢從哪裡注入無關。錄製時還是得掛全螢幕畫布才拿得到座標,還是會遮蔽,那些 app 還是會丟掉觸控 —— 連使用者自己的手指也一樣。

### 成本

- 非 root 的話,**每次重開機都要重新透過 ADB 啟動 Shizuku**。Android 11+ 可用無線偵錯自啟,但仍是門檻
- 使用者要額外安裝 Shizuku app

### 架構影響

小。`GestureDispatcher` 本來就是獨立類別,加一層注入抽象、後面掛兩種實作即可,不需要動 `Player` 或 overlay。

---

## 建議順序

**已完成**(都還沒實機驗過,見專案根目錄的 `UNVERIFIED.local.md`):

- ~~暫停點與等待成為可選取的標記~~ —— [CHANGELOG](../CHANGELOG.md)「暫停點刪不掉」
- ~~視窗配置 + 待機 / 錄製按鍵組~~ —— [CHANGELOG](../CHANGELOG.md)「工具列與播放面板不再同時出現」「每個模式各自一組工具列按鍵」。定案的結果寫進了 [SPEC §3.4](SPEC.md)
- ~~錄製時的警示區(斜線 + 文字)整個取消~~ —— 連同規格裡「標記轉黃」那個未實作的缺口一起撤掉
- ~~插入「等待 N 秒」,並與暫停點合為同一型別~~ —— [CHANGELOG](../CHANGELOG.md)「插入『等待 N 秒』」。設計理由寫進了 [SPEC §1.4](SPEC.md)
- ~~暫停點的備註~~ —— [CHANGELOG](../CHANGELOG.md)「暫停點的備註終於能填了」。入口在編輯模式的步驟設定頁
- ~~真正的復原、編輯模式只顯示選取的標記~~ —— 同一輪也把插入改成「插之前」,**後來又推翻了**:插入一律往後,方向搬到移動鍵上([SPEC §9.3](SPEC.md))
- ~~步驟清單(可捲可點)+ 跳到第 N 步 + 上下步~~ —— 100 步腳本的選取問題
- ~~從選取的那一步開始播放~~ —— 原第 3 項
- ~~編輯模式也能插入暫停 / 等待~~ —— 補上「錄完後在第 1、2 步之間插等待」這個原始情境
- ~~重錄單獨一步~~ —— 保留 `delayBefore`

- ~~編輯模式整組重做~~ —— 主選單成為容器、選取永遠存在、隔離只顯示選取的那一個、插入一律往後、`＋` 擷取真手勢、設定頁取代參數卡、列表縮成單一職責。定案的結果寫進了 [SPEC §9](SPEC.md)
- ~~設定頁的複製 / 刪除 / 前後移一格~~ —— 刪除的選取一律往前一格,方向從插入鍵搬到移動鍵
- ~~單一步驟的重複次數與間隔~~ —— 手勢與全域動作可以原地跑 N 次。少數會讓腳本**變短**的編輯功能,見 [SPEC §9.9](SPEC.md)

**編輯模式(M2)到此完整。** `多選` 與 `絕對位置搬移` 判斷為不做 —— 理由見 [SPEC §9.11](SPEC.md),它們預設你讀得懂腳本。

---

## 要做到功能完整,還缺這些

依「不需要實機就能做」到「需要新架構」排。

### A. 現有能力沒有入口

~~`GlobalStep` 做得出來但加不進去~~ —— **已補**,`⎌+` 見 [SPEC §9.8](SPEC.md)。它同時補上一個錄製缺口:無障礙 overlay 看不到導覽列,所以返回 / 首頁 / 近期永遠不可能被錄下來,只能插入。

- **[SPEC §8] 解析度 / 方向的處理** —— 目前只做線性座標縮放,方向對不上不會警告。**不做「只是警告」那一版** —— 決定改成 resize / remap,而它屬於匯出匯入那個工具:轉完要存的時候算一次,不是每次執行都算。所以這一項現在綁在下面那個網頁編輯器方向上

### B. ~~M3 — 流程~~ 已完成

- ~~流程層收成只有 `ClipNode`,補上 `delayBefore` / `repeat` / `repeatIntervalMs`~~ —— `WaitNode` / `GlobalNode` / `AwaitTextNode` 是下一層概念的複製品,拿掉([SPEC §5.3](SPEC.md))
- ~~流程展開成步驟清單交給既有 `Player`~~ —— **沒有 `FlowRunner`**。寫第二個執行器等於把每圈間隔、碰到暫停、單步重複、從第 N 步開始再實作一次
- ~~flow mode 的按鍵組(9 顆)~~ —— 它的對象是 clip 而不是 node,所以薄([SPEC §10.4](SPEC.md))
- ~~流程與工作區互斥~~ —— [SPEC §10.5](SPEC.md)
- ~~編排畫面 + 首頁流程區塊~~ —— `home_tab_flows` / `home_no_flows` 用掉了
- **`ClipDetailScreen` 決定不做** —— 匯出暫緩,而改名與刪除本來就在 `HomeScreen` 的選單裡,再開一個畫面只是多一層

### C. M4 — 條件等待與匯出匯入

- **`NodeFinder` + `AwaitTextNode` 執行** —— 開 `canRetrieveWindowContent`(不存在)
- **匯出 / 匯入 JSON** —— 上面那個網頁編輯器方向的前置
- **多指錄製與重播**:這一項**其實已經在了**。`CanvasView` 收 `ACTION_POINTER_DOWN`、`GestureDispatcher.build()` 逐 stroke 加進 `GestureDescription` 並尊重 `getMaxStrokeCount()`。只是還沒實機驗過,CHANGELOG 的 M1 條目寫的仍是「單指」

### D. 存 / 讀檔那組畫面

`WorkspaceDialogActivity` 的存 / 讀 / 開新 / 備註四個對話框都在,能用。但 **`💾` 長按 = 另存新片段**這個權宜作法還在(見本文件上方),要一起處理。

### E. 已知無解,不列在這裡

[#1](https://github.com/wz-cch/tapflow/issues/1) 注入器卡住、[#2](https://github.com/wz-cch/tapflow/issues/2) `filterTouchesWhenObscured`、[#3](https://github.com/wz-cch/tapflow/issues/3) 服務不重新綁定 —— 都標了 `known-issue`。
