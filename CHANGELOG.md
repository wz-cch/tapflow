# Changelog

格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/),版本號遵循 [Semantic Versioning](https://semver.org/lang/zh-TW/)。

## [Unreleased]

### Added — 基礎
- 規格書 [docs/SPEC.md](docs/SPEC.md) 定稿(v2.0)
- 三層資料模型:`Step` / `Clip` / `Flow`,手勢統一為 stroke 模型以支援多指
- 全域設定 `Settings`:預設時長、隨機抖動、外觀、螢幕常亮
- 語系資源:英文(預設)與正體中文
- GitHub Actions 建置流程,產出 debug APK artifact
- Git Flow 與 commit 慣例 [CONTRIBUTING.md](CONTRIBUTING.md)

### Added — M1 能錄能播
- `TapFlowService`:無障礙服務,三個懸浮視窗的擁有者
- 三個懸浮視窗:左側工具列(展開 / 圓球 / 邊緣把手)、頂部播放面板(進度與計時器)、全螢幕標記畫布
- Overlay 掛載走 `TYPE_ACCESSIBILITY_OVERLAY`,失敗才 fallback 到 `SYSTEM_ALERT_WINDOW`
- 錄製與**逐手勢補發** —— 錄完立刻送回下層 app,畫面才會前進
- `⊙` 接續錄製、`↩` 復原上一步、`💾` 存成片段(長按另存新片段)
- **暫停點**:插入即停止錄製並讓出觸控;暫停時工具列自動摺疊到螢幕上半部,把鍵盤區讓出來
- 工作區與自動草稿,服務重啟後還原
- 播放:迴圈次數、倒數、暫停 / 繼續 / 停止、音量下鍵備援(長按停止)
- 螢幕常亮、亞光黑幕、隨機位置與時間抖動、解析度線性縮放
- 標記顯示:序號準心、長按進度環、滑動軌跡與箭頭、`👁` 三段密度、步驟文字清單
- 主畫面:無障礙引導、工具列開關、片段清單(載入 / 改名 / 刪除)

### Added — M2 螢幕上直接編輯
- `✎` 編輯模式:整螢幕攔截,標記可拖曳
- 拖曳序號徽章 = 整個手勢平移;拖曳滑動箭頭尖端 = 繞起點旋轉縮放,曲線形狀保留
- 參數卡(獨立 window):時長 / 前置延遲 加減、重新指定座標、刪除
- `＋` 手動新增點擊點(套全域預設值,建立後自動選取)、`−` 刪除選取
- 全域設定畫面:預設時長、迴圈次數與速度、隨機位置與時間抖動、逐手勢補發開關、工具列大小與不透明度、計時器、螢幕常亮與黑幕
- 編輯模式強制把標記密度開到「全部」,並顯示工具列覆蓋區的斜線警示
- 懸浮工具列多一顆快捷設定鍵:迴圈次數、速度、逐手勢補發、螢幕常亮、黑幕、計時器、工具列大小與不透明度都能在目標 app 裡直接改,面板底部有連結可跳到完整設定
- 拖曳期間不寫草稿檔,放開才 flush —— 一次拖曳一次寫入,而不是每個觸控取樣寫一次

### Documented — 系統層的手勢注入器卡住(非程式碼問題)
- **診斷結果:手勢全部失敗、每次固定在 ~1010ms,是 `AccessibilityManagerService` 的 `WAIT_MOTION_INJECTOR_TIMEOUT_MILLIS`(1 秒)逾時。** `MotionEventInjector` 隨無障礙輸入過濾鏈安裝,而過濾鏈偶爾不會為新綁定重建。**重開機即恢復**,與 APK 無關 —— 同一個 APK 前一天正常、隔天全部失敗
- 辨認方式寫進 [README 疑難排解](README.md#疑難排解):**失敗時間與手勢長度無關**就是判準。106ms 的點擊和 1147ms 的滑動在同一時間點失敗,那是逾時而不是被打斷
- 新增 `GestureOutcome.INJECTOR_MISSING`,不再把它誤報成「該 app 拒收注入手勢、需要 root」—— 那句話是錯的,而且它讓這輪除錯往錯的方向走了四輪
- 加入一次性自我修復:偵測到這個特徵時重新套用 `serviceInfo`,讓系統重算 user state(那正是安裝過濾鏈的時機)。每次綁定只試一次,失敗就明講要 toggle 或重開機
- **新增診斷紀錄畫面**(主畫面最下方):記錄擷取、畫布釋放、派送座標與結果耗時,可一鍵複製。logcat 在手機上拿不到,而 toast 只能講一句話 —— 這個畫面一次就定案了前面猜了四輪的問題

### Fixed — 移除 startup 期間對 serviceInfo 的寫入
- `connect()` 原本在剛綁定時重設一次 `serviceInfo`,只為了加上 XML 裡**早就宣告過**的 `flagRequestFilterKeyEvents`。零收益,但 `setServiceInfo` 會讓系統重算 user state,而那正是安裝輸入過濾鏈的時機 —— 在綁定期間去踩它就是在跟安裝競速
- 而且原本的 fallback 會在 getter 回傳 null 時塞一個空白的 `AccessibilityServiceInfo`,其 capabilities 為 0,會直接抹掉 `canPerformGestures`。自我修復路徑也改成讀不到就拒絕執行,不自己造一個

### Fixed — 其他
- overlay 不再推送內容相同的 window 更新。呼叫端每次都從狀態重算,所以要求更新的頻率遠高於真正有變化的頻率(播放計時器每秒五次),而每一次都是真的 `updateViewLayout`
- 兩個面板的 `applyAppearance()` 原本每次刷新都重建所有子 view 的 layoutParams 並強制排版,即使大小沒變
- `setAvailableHeight()` 拿夾限前的值跟上次存的夾限後的值比,在短螢幕上永遠不相等,於是每次刷新都強制排版
- 補上斜線警示區的文字說明(規格要求但一直沒畫),條紋原本只說「不行」卻沒說原因

### Added — 工作區的存 / 讀 / 開新的
- **`💾 儲存` 會跳出命名畫面**,可以直接取名,不必存完再回 app 改。載入自片段時預設覆蓋原本那個,勾選才另存新的
- **新增 `📂 讀取`**:工具列上直接挑片段載入工作區,不用切回 app。工作區有未儲存變更時會先警告讀取會取代它們
- **新增 `⊕ 開新的`**:清空工作區開始新的一段,會先提示會清空幾個動作。原本錄完只能接續,沒有任何方式開新的
- 三者都是獨立的小畫面,一顆按鍵一件事。做成 Activity 而不是 overlay 面板,因為命名需要輸入框、輸入框需要輸入焦點,而這裡的懸浮視窗一律 `FLAG_NOT_FOCUSABLE`(否則會搶掉下層 app 的輸入法,暫停點就沒法用了)

### Fixed — 自動草稿不該無條件還原
- **重開後即使沒讀檔也會自動載入上次的內容。** 草稿的用意是保護**還沒存過**的錄製,但原本無條件還原,結果存過的片段也會自己回來,看起來像 app 擅自開檔。改成只有 dirty(有未儲存變更)才還原,存過就是乾淨的,不會回來

### Changed — 支援 Android 7
- **minSdk 從 26 降到 24(Android 7.0)。** 逐項查過,不影響任何功能:`dispatchGesture` 與 `canPerformGestures` 都是 API 24、`TYPE_ACCESSIBILITY_OVERLAY` 是 22。原本頂到 26 的只有三件事,都已處理 —— `java.time` 改用 core library desugaring、adaptive 圖示移到 `mipmap-anydpi-v26` 並在預設資料夾放一般 vector、`TYPE_APPLICATION_OVERLAY` 加版本判斷(舊版用 `TYPE_PHONE`)
- 唯一仍被 API 26 鎖住的是 `StrokeDescription` 的 `willContinue` 四參數建構子(分段 stroke,用來細緻化曲線滑動的速度變化)。它本來就是 M4 的可選項;真要做時加版本判斷,舊版退回單段 stroke
- 舊版 Android 對 overlay 的限制其實更少(沒有 Android 12 的不信任觸控封鎖),所以風險不在 API 而在測試面:Android 7 時代的 OEM ROM 對背景服務更兇,「服務不重新綁定」在那類裝置上可能更頻繁

### Removed — 第六輪實機測試回饋
- **「補發時把遮罩移開」整個移除。** 它從未被證實對被擋的 app 有幫助,卻確實弄壞了原本正常的桌面錄製 —— 打開時會出現「手勢被取消」,推測是在手勢送出前後搬動 window 會讓系統取消進行中的觸控串流。成本已證實、效益未證實,所以移除
- 補發路徑回到只切換 `FLAG_NOT_TOUCHABLE`,不再對畫布做任何搬移或縮放。會丟棄被遮蔽觸控的 app 回歸為 README 已列的限制

### Fixed — 第五輪實機測試回饋
- **逐手勢補發完全沒作用,連桌面也不動。** 上一個 commit 把「補發期間不攔截觸控」的判斷改成依據 `canvasOutOfTheWay`,而那個變數被「補發時把遮罩移開」這個**預設關閉**的設定閘控 —— 於是 `FLAG_NOT_TOUCHABLE` 從來沒被加上,補發的手勢被自己的畫布吃掉。兩件事必須分開:「不攔截」是補發能成立的必要條件,「移到畫面外」只是給會丟棄被遮蔽觸控的 app 的額外措施

### Fixed — 第四輪實機測試回饋
- **橫放時工具列超出螢幕高度,摺疊鍵摸不到。** 11 排按鍵約 480dp,橫向螢幕放不下。按鍵區改成可捲動,高度上限夾在螢幕高度內。拖曳把手刻意留在捲動區**外面** —— 放在裡面 ScrollView 會把垂直拖曳搶走,工具列就再也拖不動了
- **工具列展開後只能貼在左右兩側。** 放手時會強制吸附到最近的邊緣,拖到中間會自己彈回去。移除吸附,現在放哪就留在哪
- 「補發時把遮罩移開」的做法改成只移動 window、不改尺寸(縮成 1 像素會讓整個畫面與所有標記往右偏),並改為預設關閉的設定,快捷設定面板裡也有

### Fixed — 第三輪實機測試回饋
- **部分 app 在錄製時完全點不動。** `FLAG_NOT_TOUCHABLE` 只讓觸控穿透,不會讓 window 不算遮蔽,所以只要全螢幕畫布掛著,每個 app 收到的每次觸控都帶 `FLAG_WINDOW_IS_OBSCURED` —— 設了 `filterTouchesWhenObscured` 的 View 會直接丟棄,那些 app 連使用者自己的手指都不理。畫布改成只在錄製與編輯時掛上;待機與播放時完全移除
- 新增「補發時把遮罩移開」設定(**預設關閉**)。給「錄製時點了沒反應」的 app 用:補發前把畫布移到畫面外,讓注入的手勢不落在被遮蔽的點上。快捷設定面板裡也有,因為需不需要它取決於當下在哪個 app
  - 一開始的做法是把畫布縮成 1 像素,實機上會讓整個畫面與所有標記往右偏 —— 推測是 view 的座標空間跟著 window 尺寸走。改成只移動、不改尺寸,並且預設關閉:那個 regression 影響所有人,而它想修的問題只影響部分 app
- 「標記持續顯示」改成需要明確開啟的設定,說明裡寫清楚它需要全螢幕遮罩、會讓這類 app 忽略重播;黑幕的說明同樣補上
- 手勢被系統拒絕不再靜默:`dispatchGesture` 改回傳 outcome 而不是 boolean,分辨「拒絕」(描述無效 / 座標超出畫面 / 還有手勢在跑)與「取消」(app 拒收注入或被遮蔽觸控),兩者都會 toast 並記錄 stroke 座標範圍
- 服務啟動失敗的原因會顯示在主畫面(例外類別、訊息、第一個在自己程式碼裡的位置)。關掉再開修不了每次都崩潰的服務,叫使用者去 toggle 是錯的建議
- 啟動流程分段隔離,草稿檔解析失敗只犧牲草稿,不再拖垮整個服務

### Fixed — 第二輪實機測試回饋
- **關掉 app 重開後懸浮列開不起來。** 從最近工作清單滑掉 app 會殺掉 process,無障礙服務跟著死,而 Android 不一定會把它重新啟動。原本的 UI 只用「服務有沒有連上」判斷,分不出「從沒開過」和「開了但沒在跑」,兩者都顯示同一張引導卡而且開關是灰的 —— 已經開過的人完全沒有出路。現在讀 `Settings.Secure` 分成三種狀態、三種說明,卡住的那種會明確叫你到無障礙設定關掉再開
- 懸浮開關只要「設定裡開著」就能按,不再要求服務已綁定。它表達的是意圖,服務連上時會自己讀
- `onServiceConnected()` 加上例外防護。從這裡丟出例外會讓服務被 Android 停用,表現就是工具列永久打不開而且畫面上毫無說明
- 讀取螢幕尺寸與方向改成失敗時退回 app metrics,理由同上

### Fixed — 首次實機測試回饋
- **`✕` 收起後工具列再也叫不回來。** 原本會縮成一條 6dp 邊緣細條,實機上點不到,而 `toolbarForm` 又不會重設 —— 主 app 開關關開、無障礙服務關開,回來的都是同一條細條。移除把手形態;`✕` 改成關閉懸浮開關;每次掛載都強制重設回展開態
- **無障礙服務重連後,app 裡的開關完全失效。** service instance 被系統重用時,`teardown()` 取消掉的 coroutine scope 不會重建,所有 collector 靜默失效。scope 改為每次連線重建
- **圓球點了沒反應。** 拖曳判定一旦超過 touch slop 就永久鎖定,手指按下時稍微晃動再回來就被判成拖曳,tap 永遠不觸發。改成在放開時依淨位移判定
- overlay 位置在 view 量測完成後會再 clamp 一次,避免沿用其他尺寸螢幕的座標而停在畫面外
- 掛載失敗不再靜默,會 toast 並把開關關掉

### Changed — 編輯模式整組重做

實機試用的結論是「可以用,但體驗不好」。逐條討論後重做,**下面幾個小節有一部分被這一節取代**。

**主選單是容器,不是待機那組再加幾顆**
- 用不到的鍵**不出現**,不是變灰;總長度不是問題,按鍵區本來就會捲動
- 編輯模式移除 `💾` `📂` `⊕` `▶` `▶|` `⚙` —— 存檔、讀檔、開新片段、播放、設定都是「擁有一份腳本」在做的事,不是「修改一份腳本」在做的事
- `↩` 只在錄製與編輯出現:它存在的條件是這個模式能改動步驟,而待機能播能存卻不能改
- 編輯模式 11 顆:`✎` `＋` `⧉` `⏸+` `⏱+` `−` `↩` `⚏` `☰` `👁` `∧`

**選取永遠存在**
- 編輯模式下「沒有選取」這個狀態**不存在**:預設落在最後一步,刪除後移到接手的那一步
- 這消掉的是一整批特例,而不是一個 —— 隔離沒東西可隔離、插入與刪除沒有錨點、設定頁沒東西可開,全部來自同一個空狀態
- 點空白處因此不再清除選取:清掉會立刻被補回最後一步,讀起來像 app 自己跳走

**隔離 = 只顯示選取的那一個,沒有例外**
- 100 步、隔離開 → 第 100 個;刪掉它 → 變第 99 個
- **移除「沒有選取時顯示全部」那個退回值。** 它讓「隔離」偶爾等於「顯示全部一百個」—— 正好是它要關掉的那件事。選取永遠存在之後,退回值連要防的東西都沒了

**插入方向反過來,例外寫在按鍵上**
- 主選單的插入(`＋` `⧉` `⏸+` `⏱+`)一律插在選取**之後**。那是錄製生長的方向:選了 47,新的就是 48
- `insertBefore` 保留下來(它是唯一能表達「放到第 1 步前面」的操作),但改掛在設定頁上、**寫明「往前插」**。一顆講清楚方向的按鍵不會被誤會

**「新增一步」跟「錄製」變成同一件事**
- **`＋` 改成擷取一個真手勢**,不再在螢幕中心生一個點等你拖過去 —— 那是兩個動作換一個點,而且兩個都不是你已經會的那個動作
- 錄製 / `＋` / 「往前插」/ 「重錄」共用同一條擷取路徑,後面只差落點:附加、插入、取代。`Capture` 這個 sealed interface 就是那三種處置
- 順帶解掉原規格「手動新增只做點擊點」的限制:手動加的一步現在可以是錄製能產生的任何東西
- 移除 `tapStep()`

**設定頁取代參數卡**(`ParamCardView` → `StepPanelView`)
- **置中的大視窗,刻意跟標記的位置無關。** 它顯示的是數字和按鍵,貼在標記旁邊只會讓它變小、擋住標記、還跟列表撞在一起
- **不再是「選到就跳出來」**,要按 `⚏` 才開。拖標記是編輯模式最常做的事,而面板每次選取都彈出來正好擋著它
- 多了**上一頁 / 下一頁**與**跳到第 N 步**(從列表搬過來)
- 多了**「等待」那一列** —— 在它之前,等待的秒數只能在插入的那一刻設定一次,因為面板對沒有座標的步驟不顯示任何一列,而那正好就是等待。下限一秒:0 秒是等待與手動暫停的分界
- 移除**「重新指定座標」** —— 編輯模式可以直接把標記拖過去,而重錄涵蓋了拖曳做不到的部分
- 移除**「刪除」** —— 它作用在選取上,而它在主選單。同一個動作放兩個地方,兩邊遲早會長出不一樣的行為

**列表縮成單一職責**
- 也改成置中的大視窗,**跟設定頁互斥** —— 開一個就關掉另一個
- **不可拖曳了,也不需要。** 拖曳把手當初存在只為了閃避它擋住的東西,而它會擋住東西是因為跟參數卡同時在畫面上
- 點一列 = 選取 + 關列表 + 開設定頁。列上刻意沒有複製或刪除:那些作用在選取上,在主選單
- 移除上一步 / 下一步(可以滑可以點,再多一組不會更快)與跳到第 N 步(搬到設定頁)

**刪除選取後接手的是鄰居,不是最後一步**
- 刪掉第 47 步,選取落在接手的那一步;刪掉最後一步,就是變成新的最後一步。若交給「退回最後一步」處理,刪掉 47 會讓隔離模式下唯一的標記跳到腳本尾端

**一次只有一個大介面** —— 設定頁、列表,或乾淨的畫布。實機第一個抱怨就是重疊,而互斥比教兩個視窗互相閃避便宜

**其他**
- 擷取手勢期間兩個大視窗都收起來:擷取要全螢幕,而視窗會擋住它要的那個手勢
- 關閉懸浮工具列時明確 remove 每一個 window。工具列一掉,`syncOverlay` 就提早 return,留在上面的視窗會沒有東西能收掉它

### Changed — 編輯的抓取點改成三個,每一個都名符其實(實機回饋)

實機回饋是「不好點,而且不明確要點哪」。第一版的反應是把標記畫大 1.5 倍 —— **沒有用**,而且方向錯了。整節設計理由寫進 [SPEC §9.10](docs/SPEC.md)。

- **問題從來不是尺寸,是徽章的意思不對。** 它是一個畫在起點上的圓點 —— **看起來**是「起點」,**行為**是「整段移動」。看到的和做到的不一致,放大只會讓它更容易按錯
- **1.5 倍放大整個撤掉。** 圓點標的是**一個像素**,30dp 在說謊;而且 100 個標記一起放大會讓重疊更嚴重,正好是隔離要解決的問題
- **改成三個抓取點**:頭的外圈改起點(新增 `withStartAt()`,`withEndAt()` 的鏡像 —— 繞末端而不是繞起點做旋轉縮放)、尾的外圈改末端、**外接矩形框內整段移動**。兩個外圈之間誰近誰贏,外圈贏過框
- **本體用外接矩形,不是「到曲線的距離」。** 原規格砍掉本體抓取點的理由是路徑 hit-test 的成本與歧義 —— 矩形兩個都沒有,而且**畫得出來**,目標不再是隱形的。角框,監視器物件辨識那種樣子
- **「本體會有歧義」那條當初是在隔離還不存在時寫的。** 隔離開著時畫面上只有一個標記,跨標記歧義從架構上就沒有了
- **退化情況不需要特例。** 沒有可分辨末端時只有一個抓取點,而它是「整段移動」—— 單點、長按、多指、短到兩端分不開的滑動全走這條。因為單點沒有「起點 vs 整段」的區別。實作上這也是必要的:單點若走 `withStartAt` 會拿不到旋轉中心,回傳一個兩點的滑動 —— **步驟型別會因為你推了一下而改變**
- **「框」其實是兩種東西,分開講才對:** 「觸控圈」是因為圓點標的是一個像素、太小點不到,所以給它一個真正按得到的範圍 —— 起點、終點、單點**全部是同一件事**;「線條框」才是框住路徑、當作「整段移動」的範圍
- **單點只有圈、沒有框。** 沒有線可以框,而跟圈一樣大的框只是在同一個東西外面再畫一圈輪廓
- **觸控圈的意思是「點得到」,不是「拖得動」** —— 所以暫停 / 等待 / 系統按鍵**也有圈**。它們拖不動,但非常需要點得到:選到它是刪除它的唯一途徑。第一版不給它們圈,理由是「圈承諾了一個不存在的握把」,那是把圈的職責讀錯了
- **短滑動的本體原本完全抓不到,那是 bug。** `hitTest` 先測末端、命中就回傳,兩者共用一個半徑;`SWIPE_TRAVEL_PX` 是 24 個**原始像素**,3× 螢幕上 travel 只要 8dp 就算滑動,末端於是落在另一個目標裡吃掉每一次觸控

### Added — 抓取外圈大小可以自己調
- **全域設定多一項「抓取外圈大小」**(bar + 數字,預設 52dp,範圍 36–96dp)
- **一個旋鈕決定三件事**,所以是一個設定而不是三個常數:畫出來的外圈、編輯模式的抓取半徑(外圈的意義就是「把抓取半徑畫出來」,所以必須相等)、以及兩端要相距多遠才各給一個外圈(一個外圈的直徑 —— 更近就沒有各自的地盤)
- **做成設定而不是調好的常數**,因為手指大小差很多,這個數字紙上算不準。預設值取的是可調之前那個硬編碼半徑,所以不動它的人行為完全不變

### Added — 系統按鍵終於插得進去(`⎌+`)
- **編輯模式與錄製中多一顆 `⎌+`**,先問哪一個(返回 / 首頁 / 近期 / 通知)再插在選取之後
- **這不是便利功能,是唯一的入口。** 無障礙 overlay 看不到導覽列,實體按鍵也不是觸控事件,所以**返回鍵永遠不可能被錄下來**。在這顆鍵之前,`GestureDispatcher` 播得出來、`StepText` 有文字、`RepeatableStep` 連重複次數都支援 —— 卻沒有任何地方能建立一個,是一段只能靠匯入 JSON 才碰得到的能力,而匯入還沒做
- **錄製中按下去會真的執行那個動作。** 理由跟逐手勢補發完全一樣:下層 app 必須跟著你往前走,否則這一步之後錄的每一步都是對著錯的畫面錄的。一樣尊重「逐手勢補發」開關;編輯模式**不執行**,跟 `＋` 和「重錄」同一條規則
- **新增 `OptionPadView`** —— `NumberPadView` 的姊妹,理由相同:要先問的插入必須問在 overlay 上,Activity 會把正在錄的 app 推到背景,而那正是下一步要落在的畫面。它不知道選項的意義,呼叫端給標籤、拿回索引
- **`GlobalStep` 現在有標記了**(`MarkerKind.GLOBAL`,綠色箭頭圓點),跟暫停同一套推導位置的處理 —— 在此之前 `buildMarkers` 對它回傳 `null`,所以就算存在也選不到、刪不掉
- 設定頁的標題對全域步驟顯示**是哪一顆鍵**,不是籠統的「動作」。四種在畫布上長得一樣,標題和列表文字是分辨的地方
- `GlobalKind.toGlobalActionId()` 從 `GestureDispatcher` 的 private 提為 top-level —— 現在兩處要執行它(重播、以及錄製中插入),同一份對照表不該有兩份

### Added — 從流程裡點片段就能編它,完成就回到流程

在流程編排裡看到第四個片段的 delay 不對,原本要離開流程、在首頁找到那個片段、修好、再回來找那條流程。現在**點片段名字就打開它**。

**片段開在流程之上,關閉就退回去** —— 深度一層的堆疊,跟參數卡蓋在畫布上同一個形狀,也帶來同一個結果:巢狀時只有一個出口。

- **模式鍵在巢狀時隱藏,這是重點不是整理。** 留著的話它有兩個讀法(兩邊都清空 / 回到我來的那條流程),而決定是哪一個的是「你怎麼進來的」—— 看不見。一顆鍵兩個意思由隱藏狀態決定,正是當初逼出「mode 要變成明確狀態」的那個 bug。所以巢狀有自己的鍵,圖示刻意不像那顆關閉
- **完成會問要不要存,而它是唯一能提供「存」的丟棄問句。** 一般那句不能,因為存新錄製需要名字、名字需要 Activity。而走到這裡代表片段是**被載入的**,已經有名字有 id,覆寫不用打字 —— 兩個標籤在 pad 上,沒有狀態機。工作區乾淨就整個跳過
- **「另存新檔」在巢狀時隱藏,因為那裡它是陷阱。** 流程用 id 參照,另存新的會讓流程繼續指著沒改過的原件 —— 你的修正看起來完全沒生效
- **書籤不是 `currentFlowId`,而且不能是。** 那一個說播放鍵跑哪條流程,設它就會讓「載入的流程」和「dirty 的工作區」同時存在兩側 —— 正好是 `Session` 要防的事。它是導覽歷史,私有於 `Session`,每個入口都清、只有一處設,沒有任何決定 mode 的邏輯讀它
- **進去不問任何事,而那不是漏掉的**:流程編排每改一下就寫檔,而 flow mode 下工作區恆為空,兩側都沒有東西會丟
- 重新推導可見集合時抓到一個自己造的洞:完成鍵在編輯模式下仍可用(「修好 node 再回去」就是整個情境),但模式鍵是靠「在編輯模式隱藏」避開這件事的 —— 所以回去會在畫布還攔著整個螢幕時改掉 mode。`backToFlow()` 先離開編輯模式
- 兩條失敗路徑:**存檔失敗就留在片段上**(不走回流程、把修改留在不再解釋它的畫面後面);**流程被刪掉**就落在 flow mode 並說一句

### Added — 存檔資料夾可以分類:進得去任何子資料夾

那個資料夾是使用者的,裡面怎麼排是他的事。片段現在可以在根層,也可以在他自己開的任何子資料夾裡。

**型別因此必須離開路徑。** 原本是目錄(`clips/` / `flows/`),而檔案能放在任何地方之後,父目錄就不再說明檔案是什麼。改成副檔名 `.clip` / `.flow`。

**這順便消掉了規格自己的一個例外。** §12.2 早就定了「`id` 在 JSON 裡才權威,檔名是裝飾性的」,而型別卻藏在路徑裡 —— 位置對某一件事是承重的。現在**檔案自我描述,位置對 app 完全沒有意義**。

- **一次走完,兩種一起出來。** 走樹是昂貴的部分,而每個要 flow 的畫面都要 clip,所以分兩次呼叫就是同一批目錄走兩遍 —— SAF 上每個目錄一次 IPC 往返
- **上限 200 個目錄,超出的數量會被回報。** 有上限但不靜默 —— 靜默的上限會被讀成「你的片段庫就這些」。同一個計數器順便擋掉 symlink 迴圈
- **遞迴讓「掃得完整」變成播放的正確性要求,而這不是潔癖。** 流程用 id 對記憶體那份庫解析,所以**一個進不去的目錄不會表現成「資料夾讀不到」,而是某條流程說它的片段不見了**。所以那個數字現在把「進不去的目錄」也算進去
- **讀取畫面會導覽**:資料夾在上、當前模式那一種在下。「上一層」是從路徑推的,不是拜訪堆疊 —— 路徑**就是**歷史,兩條路徑進到同一個資料夾時沒有東西會走散
- **列是從記憶體篩的,不是每個資料夾再讀一次。** 庫是整份載入的(流程要用 id 找片段),再讀一次是做過的工,而且兩份可能不一致
- **存檔會說它要存到哪,而且只在「那是個選擇」的時候才能改。** 覆寫去檔案原本的位置 —— 那是 `write()` 的承諾 —— 所以在那裡給一個選項等於給一個不會生效的控制。新檔案去一個記住的工作資料夾
- **首頁維持一份平的清單**(一次看到全部是它的用途),但每一列會說它在哪個資料夾 —— 看不見的話,把片段分類看起來就像把它弄丟了
- **位置對 app 依然沒有意義。** `folderOf()` 只有兩個列清單的畫面在讀,`FlowPlan` 從來不問片段住在哪。這是**改名與「刪除會從每條流程剪掉」完全不用改**的原因
- **`.clip` 差點活不下來。** `ExternalStorageProvider.createDocument` 會從 MIME type 推副檔名然後接上去,所以 `application/json` 會把 `登入.clip` 變成 `登入.clip.json` —— 寫進去了但認不出來,可用結果裡最糟的一個。改用 `MimeTypeMap` 不認識的自訂 type,名字才原封不動;`Kind.matches()` 兩種都收
- 驗證時抓到 `write()` 的合約有一句過度承諾:SAF 上認得的文件不見了時會**重建**,而重建在工作資料夾(document Uri 不交出父層,沒有東西可以「重建在它旁邊」)。所以一次需要重建的存檔**可以**搬動片段。文件化而不是修掉 —— 替代方案是為一條只有「存檔時檔案剛好被刪掉」才走到的路把資料夾穿過 `remember()`

### Fixed — 存檔在 Android 10 直接當掉,而線索是一個超過檔案長度的行號

`IllegalAccessError: Field 'Clip.Companion' is inaccessible to class 'Repo'`,而 stack frame 指著 `Repo.kt:465` —— 那個檔案只有 413 行。**那個行號就是唯一的線索**:Kotlin 的 inliner 會為 inline 進來的程式碼發出超過檔尾的合成行號,所以出事的不是那一行,是被 inline 進去的東西 —— `encodeToString`。

原因是 `Clip` 宣告了一個 `private companion object`。**serialization 的 compiler plugin 會把 `serializer()` 掛在那個類別的 companion 上,而如果類別自己宣告了一個,那就是同一個。** 於是 `Clip.Companion` 是 private,每一次 `encodeToString(clip)` 都編譯成「從別的類別讀一個 private 靜態欄位」:**編譯得過**(plugin 產生在 inline 函式的 body 裡,沒有 synthetic accessor),然後在**真的會檢查的 runtime 上**炸掉。Android 11 放行,Android 10 不放行 —— 所以它表現成裝置專屬的 crash 而不是 bug。

常數移到 top-level `private const val`。**用 bytecode 對照證明,不是推論**:修之前 `private static final Clip$Companion Companion`,修之後 `public`。規則寫進 [CONTRIBUTING.md](CONTRIBUTING.md):`@Serializable` 的類別不可以宣告 private companion。

### Fixed — 存檔會凍住 UI,以及當機之後什麼都不留

- **資料夾的工作全部丟出主執行緒。** 存檔在 SAF 上是一次 ContentProvider 往返,而 provider 慢的時候表現成整個 UI 卡住,不是「存檔花了點時間」。`DocumentFile.findFile()` 順便會列舉整個目錄
- **`CrashLog`**:自己的 `UncaughtExceptionHandler`,把 stack trace 加上事發前的 `Diag` 時間軸寫進 `filesDir/last-crash.txt`,並且**接回原本那個 handler**(不然就吃掉了系統的當機處理)。首頁用錯誤色說一句,診斷畫面可以複製 —— 一份沒有人知道要去看的當機報告等於沒有

### Added — 看得出來現在跑的是哪一版

兩輪除錯花在問「這是我剛傳的那個 APK 嗎」,而答案只能靠去 repo 查一個 sha。**那是工具的缺陷,不是記性的問題。**

三件事同時錯:本機建置全都自稱 `0.1.0-local`;CI 的 artifact 全都叫 `app-debug`(所以兩次下載在下載夾裡長得一樣 —— 一個一週前的版本就是這樣被裝上去然後拿來回報的);而版本號只有 sha,它精確地識別一個建置,卻對人什麼都沒說。

現在是版本**加時間**:`0.1.0+8f6ab6f · 2026-08-04 08:31`。時間戳是真正解決問題的那一半,因為「APK 什麼時候到的」大家本來就知道,不需要查任何東西。**驗證過它真的每次建置都會變**,而不是假設。

### Changed — 片段就是檔案:資料夾整個拿掉

**做了一版「授權一個片段庫資料夾」,然後整個換掉,而換掉的理由是它解不了它自己要解的問題:** 存取要事先安排,所以存檔只能存到那**一個**資料夾;而最致命的是**一條流程沒辦法同時引用兩個資料夾裡的片段** —— 那正是資料夾的用途,`common/delay.clip` 搭配 `battle/stage3.clip`。

**現在片段就是一個檔案,在你放它的任何地方。** 打開它就是讀檔,而 app 手上只有「現在開著的東西」。設計理由見 [SPEC §12](docs/SPEC.md)。

- **`Clip.id` 與 `Clip.name` 都拿掉了。** 位置就是身分,檔名就是名字。`id` 放在 JSON 裡的後果是:複製一份檔案會被認成**同一個**片段,而改名會被認成同一個 —— 兩件都跟任何人對待文件的直覺相反。名字同時存在檔名與內容裡就是同一個名字兩份,而其中一份使用者在 app 外面隨時可以改。`createdAt` / `updatedAt` 一起拿掉,理由平淡得多:從來沒有任何畫面顯示它們
- **流程指的是位置。** 片段被搬走或改名,那一列就變 `!`,旁邊是「重新指定檔案」,而選擇器**從流程自己所在的位置開始找**。這不是待修的缺陷:從檔案管理員改名攔不下來,所以讓 app 內的改名去偷偷修好參照,只會做出一個沒人能依賴的差別。而用名字比對更糟 —— 會靜默抓到一個剛好同名的別的片段
- **開流程 = 把它的片段讀進記憶體一次(snapshot)。** 之後改那些片段不影響已經開著的流程,要看到修改就重開 —— 兩邊開同一個檔的文字編輯器就是這樣。所以編排畫面**每次進入都重讀**,因為從片段編輯回來正是那個修改該出現的時刻
- **四個入口一個 `FilePicker`**:讀取、另存、流程的 `＋`、`!` 重新指定。API 29+ 用系統文件選擇器(有搜尋、有其他 provider);**API 28- 自己瀏覽**,因為那台 Android 7 的系統選擇器一個儲存根目錄都列不出來
- **`💾` 沒有任何對話框了**,直接覆寫來源檔;沒有來源檔就自動變成另存。原本它會問名稱、問要不要另存為新的、還要瀏覽資料夾 —— 三個問題,答案幾乎永遠是「跟上次一樣」。另存變成自己一顆鍵(`💾+`)
- **首頁列最近開過的 20 個檔案,摘要是快取的**,所以畫面**完全不做 IO**。快取的是數字而不是那句話 —— 預先算好的「3 個動作 · 12 秒」會把語系凍進去。檔案不見的那幾列在背景檢查後變灰掛 `!`,而那個旗標**不寫進檔案**:上週讀不到的 ref 今天可能是一張掛好的記憶卡
- **`⋮` 裡「從清單移除」與「刪除檔案」是分開的兩件事**,而那個差別就是這個模型的意義。刪片段**不去動任何流程** —— 原本會從每條流程裡把它剪掉;現在那些列變 `!`,保住排列而不是靜默把流程變短
- **流程裡的片段,工具列從 12 顆縮到 6 顆。** 單獨播流程裡的第四個片段,是拿它去對「現在螢幕上長這樣」跑,所以驗證不了任何事;而 `📂` 與 `⊕` 更糟 —— 它們**會動**,而在動的同時靜默結束了巢狀狀態。編輯那一組整組保留,編輯就是它存在的目的
- **拿掉的東西**:資料夾關卡、遞迴走樹與 200 目錄上限、id → 位置對照表、子資料夾瀏覽器、工作資料夾設定、存檔對話框的名稱欄與「另存為新的」勾選、「有 N 條流程在用這個片段」
- **沒有搬移機制,因為還沒 release。** 舊的片段檔照樣 parse —— 裡面的 `id` 與 `name` 只是被忽略

### Fixed — 副檔名不該出現在打字的地方(實機回饋)

`.clip` 原本是**建議檔名的一部分**,所以系統選擇器的檔名欄裡就擺著 `錄製 08-04 10:30.clip` —— 一個承重的東西被做成裝飾,任人編輯或刪掉。

**現在建議名字是乾的,副檔名在檔案建好之後補上。** 補的方式是 rename,而不是請選擇器加:不認識的 MIME type 是 `ExternalStorageProvider` 不亂加副檔名的原因,而同一個不認識也代表它不會幫我們加。provider 不支援 rename 就只損失那個副檔名,檔案照樣讀得開。

**而「開片段時只看到片段」在 API 29+ 做不到,這是平台限制。** 選擇器按 MIME type 過濾,`.clip` 與 `.flow` 都沒有註冊過的 type,所以兩種都回報 `application/octet-stream` —— 分不開。而濾那個 type 會**藏掉** `.txt` / `.json`,也就是一個被複製成 `notes.txt` 的片段會在選擇器裡消失:藏掉使用者需要的檔案比列出太多更糟。

所以能做的是**選錯的時候把話講清楚**:開檔失敗後再解析一次,於是「這是流程檔,不是片段檔」跟「這個檔案壞了」不再長得一樣。API 28- 沒有這個問題 —— 那邊是自己列的,直接按副檔名濾。

### Added — 一步沒送到的時候要怎麼辦,自己決定

原本兩種失敗各有一條寫死的規則,而且**是反的**:取消永遠暫停且關不掉;拒收與注入器不見永遠繼續,播放中連 toast 都不出。也就是最可能救得回來的那種會停,而「手勢根本沒送到」的那兩種會走下去 —— 一份 40 步的腳本可以跳掉 38 步然後回報跑完了。

拆成兩個設定,因為那是**兩件不同的事從同一個 callback 回來**:

- **你碰到螢幕時:中斷 / 暫停 / 忽略。** 真實輸入一到,framework 立刻取消進行中的注入手勢 —— 兩個輸入流不會合併,真實輸入直接贏 —— 所以下層 app 收到的是**我們送出的那一部分 + 你的手指,兩個都收到**。這讓它的性質是「有人伸手接手」而不是故障,也是為什麼三個選項裡沒有重試:重發等於把半個滑動疊在自己上面。別家大多是忽略,主要因為它們偵測不到;我們偵測得到,所以這個選擇是真的
- **完全沒送出時重試 N 次,然後略過 / 暫停。** 這裡什麼都沒送到,沒有半套觸控要考慮,重試是安全的

**兩個都預設暫停** —— 取消那一邊維持原行為,另外兩種改掉。

**重試上限 10,讀進來就夾。** 注入器不見不會自己好(要 toggle 服務或重開機),所以每一次重試都會失敗,每次還要付 framework 那一秒的逾時。

**略過會回報。** 選略過的意思是「少一下點擊沒關係」,而那個前提只在你相信那些點擊發生過時成立。所以次數在結束時講一次 —— 不打斷是選它的目的,不被告知是另一回事。

`GestureDispatcher` 自己那兩個重試留在原地(重新註冊服務後試一次、取消得太早所以什麼都沒送出時試一次)。那兩個是辨認出特定故障然後修它;這個迴圈是表達偏好,坐在它們上面。

### Added — 設定的數字可以打字,不用只能拖

**迴圈次數是逼出這件事的那一個。**「永遠循環」是 0,0 是 0–500 滑桿最左邊那一個像素,而「永遠循環」四個字要滑到才顯示 —— 設定一直都在,只是找不到。所以無限循環不需要新功能,只需要能把數字說出來。

十列的數值變成可點(強調色,一行式的列只塞得下這一種提示)。

**逐列選擇加入,不是全部自動,而這是它的設計重點。** 有四列**顯示的數字不是儲存的數字**:速度與工具列大小顯示倍率,不透明度與黑幕濃度把 0–1 的分數顯示成百分比。一個直接接到滑桿單位的輸入框會把不透明度的「85」寫成 85 而不是 0.85,**靜默地**。所以 `TypedNumber` 用顯示的單位給值、自己帶轉換回去,而兩種單位不一致的那幾列乾脆不提供 —— 它們範圍很窄,窄範圍正是滑桿擅長的。

- **打進去的值照原樣採用,不套 granularity。** granularity 是為了讓**拖**落在整數上;打字就是繞過它的手段,把 137 ms 圓成 150 會把唯一一個刻意精確的輸入丟掉
- **超出範圍是拒絕不是夾**,而且把上下限講出來。夾會把打錯的 5000 變成看起來被接受的 500,而那一列本來沒有任何地方寫著上下限
- 流程自己的迴圈次數也有,而且打字**穿過滑桿的天花板** —— 滑桿停在 100,模型允許到 9999

### Fixed — 實機回饋:編輯模式那兩個點不到的地方

**未儲存提示有兩顆取消。** `OptionPadView` 底部本來就有自己的取消(每個 overlay pad 都靠它關掉,它一直都在),而 `confirmDiscard` 又把取消當成**選項**傳進去,所以畫面是 `確定 / 取消 / 取消`。五個 pad 呼叫端只有這一個這樣做 —— 同一個檔案兩百行外的流程刪除早就是對的形狀:一顆講出破壞性動作的標籤,加上 pad 自己那一排。剩下那顆改成講出結果(「丟棄並繼續」),因為取消消失後它要自己承擔全部語意,而「確定」對著「有修改沒儲存」並沒有說它答的是哪一邊。

**編輯模式點等待 node 沒有反應**,於是秒數看起來像編輯器改不了的東西。其實一直改得動(`onEditWaitSeconds` 加數字盤),但唯一的入口是參數卡,而參數卡只從工具列那顆鍵上來。**現在點任何標記就開參數卡。**

- 為什麼是在等待 node 上發現的:手勢標記至少會被**拖**,而等待、暫停、系統按鍵是刻意不可拖的(座標從鄰居推算,拖了不會動,下次重建又跳回原處),所以那三種的一次點擊回饋是**零**
- 參數卡現在是 **modal:下面不能操作也不能移動**。它是獨立 window,不做 modal 的話畫布會在**卡片沒有實際蓋到**的地方繼續收觸控 —— 一邊打參數一邊把標記拖走,而且「這個標記不能拖」跟「這個標記在卡片底下」分不出來。這兩件事改動前就成立,自動彈出只是把它們從偶發變成日常
- 觸控是**吃掉**而不是放行:編輯時畫布蓋滿整個螢幕,放行等於把觸控送給正在被寫腳本的那個 app
- **換步與出口都不用新做**:卡片本來就有 `‹ › ✕`。所以「開著」這個狀態自己就夠用 —— 走前後步不必關,要選另一個標記才關
- 只有畫布這條路開卡片。`select()` 還有七個呼叫端(刪除落在鄰居、複製落在複本、列表、翻頁鍵),沒有一個是「我要看參數」

### Fixed — 讀不出來的存檔不再靜默消失

跳過解析不了的檔案是對的 —— 那些檔案在使用者會打開、會複製的資料夾裡,所以一個壞檔必須只花掉那一個片段而不是整份清單。**靜默**跳過不對,而那個靜默正是實機上那次遺失的形狀:非正常關機,下次啟動片段庫是空的,什麼都沒說。

從首頁看,「檔案壞了」跟「這個片段從來沒存過」完全一樣,而兩者要做的事相反 —— 壞掉的那個檔案**還躺在資料夾裡**,檔名還是你取的。

數量是**減出來的**:列到的檔案數減掉解析成功的物件數。不放計數器進 `decode()`(它沒有理由知道自己在被統計,而獨立的計數器會跟它描述的清單走散)。顯示在兩個會少東西的地方:首頁清單上方與 `📂` 讀取清單上方,錯誤色。

還留著一個更窄的靜默:**完全讀不出來**的檔案(I/O 錯誤而非內容壞掉)在 `LibraryStore.list` 裡就被丟掉,不會進到那個減法。那需要介面本身回報,而介面正要為「可瀏覽的資料夾」重寫,所以歸到那次改動。實機遇到的那一種落在這裡 —— 被截斷的檔案讀得出來,是解析失敗。

### Fixed — Android 7 完全無法指定資料夾,於是舊版本改走真實路徑

一台 Android 7 上 `ACTION_OPEN_DOCUMENT_TREE` 只列得出「最近」,而「最近」列的是檔案,所以對挑資料夾的選擇器來說永遠是空的 —— 整個功能在那台無法設定,而 Android 11 那台完全正常。試過 `SHOW_ADVANCED` 沒有用,那台的 DocumentsUI 沒把 `ExternalStorageProvider` 的根目錄露出來,而剩下的路都不通:可持久化授權只能由選擇器回傳,`EXTRA_INITIAL_URI` 是 API 26 才有的。

**但 Android 9 以下根本不需要 SAF。** Scoped storage 是 API 29 才開始的,在那之前 `WRITE_EXTERNAL_STORAGE` 就是完整的檔案存取。所以按版本切成兩個後端:

- **API 29+** `SafLibrary` —— 使用者授權的資料夾,照原樣
- **API 28-** `FileLibrary` —— 固定 `/sdcard/TapFlow`,**由我們建立**,沒有選擇器

分界線在 28/29 而不是 29/30:Android 10 還能要求 legacy 存取,但那個旗標 Android 11 就不看了,釘在平台真正改變的地方才不會有一個「行為取決於可能被拒絕的請求」的後端。

**舊後端比新的簡單**,這跟直覺相反:沒有 id → Uri 對照表(路徑就是位置)、沒有 `"wt"` 截斷坑(`writeText` 本來就截斷)、沒有 `createDirectory` 生出 `clips (1)`、沒有會被卸載收回的授權。而且**我們可以建資料夾** —— 那個「外層要你自己建好」的限制純粹是 SAF 的。

「可以自己挑資料夾」從來不是目標:重裝不消失、放在能自己備份的地方才是,而固定路徑兩件都滿足。代價是舊裝置跳一次執行期權限;manifest 用 `maxSdkVersion="28"` 宣告,新裝置完全不會看到。

### Added — 存檔放使用者指定的資料夾,重裝不再消失

`filesDir` 隨卸載一起消失,而 Android 內建的 `allowBackup` **本來就開著**卻救不了:還原只發生在安裝那一刻(手動裝 APK 不會觸發),備份綁簽章憑證(debug 與 release 不同),上傳時機是充電 + 閒置 + 非計費網路約一天一次。設計理由寫進 [SPEC §12.1–§12.8](docs/SPEC.md)。

**一份,不是兩份**
- 使用者指定的資料夾裡那一份就是唯一一份。**不做內部鏡像** —— 鏡像會製造一個兩份不一致而且沒有人能裁決的狀態:資料夾看起來權威卻可能比 app 顯示的舊,重裝後從它還原就靜默拿到舊資料
- 外部只在**存檔、讀檔、顯示清單**時被碰。錄製、編輯、播放工作區完全不碰,所以「外部比較慢」的代價只在使用者主動要求的地方付
- 工作區草稿與設定留在內部:一個每一步都寫,一個是這台裝置的物理屬性(抓取外圈、工具列大小)

**檔名是裝飾性的,這一個決定讓三個問題不存在**
- 身分是 JSON 裡的 `id`,檔名只是片段名稱,讀取時完全不看檔名。所以同名讓 provider 自己加後綴、非法字元清掉就好、而**改名時檔案改名失敗只是標籤過期,不是存檔失敗**
- 另記一份 id → 文件 Uri 對照表,因為 `findFile()` 每次都會列舉整個目錄

**三個 SAF 的坑**
- **`"wt"` 而不是 `"w"`** —— `"w"` 不保證截斷,較短的 JSON 寫在較長的上面會留下前一份的尾巴。**不是機率問題**:刪一個步驟就會讓檔案變短
- **先 `findFile` 再 `createDirectory`** —— `ExternalStorageProvider` 對已存在的名稱會給你 `clips (1)`,一半的片段就從清單消失
- **Android 11+ 不讓選 `Download` 或內部儲存根目錄** —— 引導文案得說「請選一個子資料夾」,否則系統拒絕會被讀成這個 app 壞了

**不在首次啟動強制指定**
- 一個還沒被試過的 app 前面擺檔案選擇器是收費站。改成按 `💾` / `📂` 時沒有才問
- 這順帶關掉一個漏洞:強制引導擋不住服務(開機後它自己重新綁定),會有一個背後沒有儲存空間的工具列。錄製不需要資料夾,只有存檔需要
- **沒有搬移機制。** 選有東西的資料夾那些就是你的,選空的就從空的開始

**存檔失敗的安全網已經存在**
- `commit()` 三種結果而不是兩種。原本「失敗」跟「沒有東西可以存」共用一個 `null`,所以資料夾讀不到那天訊息會說「沒有東西可以存」—— 那是事實的反面
- **`dirty` 只有寫入落地才清掉**,所以失敗時工作區仍然未儲存,復原草稿自動接手
- 「資料夾可用」是一個 flow 而不是布林欄位,所以正在存檔的畫面會自己變成「重選資料夾」

**量過的大小,不是猜的**
- `prettyPrint` 關掉:一個 100 步片段 250 KB,20 個 pretty 是 5.5 MB、compact 是 1.0 MB(**5.3 倍**)。滑動貴是因為每個取樣點 pretty-print 後約 200 B
- **一個片段一個檔**也是這個數字決定的:存一個寫 250 KB 而不是整個 5.5 MB,少 20 倍;寫壞了只損失一個片段
- **沒做 temp + rename 原子寫** —— 有了一個片段一個檔,最壞情況已經從「全部沒了」變成「一個壞了」,而 `renameDocument` 各家 provider 行為不一致

**服務永遠不需要叫出資料夾選擇器** —— 它唯一會寫外部的時機是「某個東西已經被載入」,而那代表資料夾選過了。`💾` 和 `📂` 都走 Activity。

### Fixed — 流程迴圈滑桿每動一格就寫一次檔

`FlowEditorScreen` 的迴圈滑桿在每個 value change 都呼叫 `Repo.upsertFlow`,也就是一次拖曳幾十次寫入。寫內部的幾 KB 看不出來,寫使用者資料夾就是幾十次 ContentProvider 往返,滑桿會跟自己的存檔打架。改成放手才寫。(片段設定那三個滑桿本來就是本地狀態、按確定才寫,不受影響。)

### Changed — 模式變成明確的狀態,首頁不再是啟動器

**根因是一行推導。** `flowMode` 原本就是 `Repo.currentFlowId != null` —— 模式不是一個可以設的東西,而是「有沒有載入流程」的同義詞。那一行製造了整串怪異:「在 flow mode 但還沒挑流程」無法表示(所以第一條流程建不出來)、每次讀取與卸載與錄製都在偷偷改模式、「切換模式」根本無法表達。設計理由寫進 [SPEC §10.4–§10.8](docs/SPEC.md)。

**模式**
- `AppMode.CLIP | FLOW` 是持久化的狀態;`currentFlowId` **不再持久化**。啟動時回到上次的模式,但**兩邊都是空的** —— 錄到一半掉了救不回來,流程重挑一下就好
- 工具列最上面、拖曳把手底下多一顆**模式鍵**,那是唯一刻意切換的路。位置是規格的一部分:它是唯一會改變其他按鍵*含義*的鍵,所以周圍換組時它不能位移
- **圖示顯示你現在在哪個模式**,不是按了會去哪 —— 顯示目的地的切換圖示在你不記得方向的那一刻就是歧義的
- **切換把兩邊都清空**,所以 `clip → flow → clip` 回到空的 clip mode。這讓「按播放會發生什麼」單看模式就能回答
- **錄製鍵在 flow mode 拿掉** —— 它原本兼任「離開 flow mode 的路」,那是一次隱藏的模式切換
- **`📂` 只列當前模式那一種。** 兩種都列在模式明確之後是個破口:在 clip mode 挑一條流程等於沒經過模式鍵就清空了工作區
- `engine/Session.kt` 是「什麼被載入了」的唯一擁有者。不變式是**兩邊最多只有一邊有東西**,散在呼叫端就是一組要記得的邊 —— 上一輪的 bug(讀片段忘了卸流程)正是漏了一條

**丟棄工作區:一個問句,五個地方**
- 工具列 `✕`、首頁的工具列開關、模式鍵、`📂` 讀另一個、`⊕ 開新的` —— 同一個函式、同一句話、**確定 / 取消**。前兩個原本完全不問
- **條件是 `dirty` 而不是「有沒有步驟」。** 讀進來沒動過的片段已經是存好的,為它跳警告會把「按掉警告」訓練成反射
- **沒有「存」這個選項。** 存一個新錄製需要名字→需要 Activity→每個呼叫端都得把待辦動作帶出去再帶回來。取消再按 `💾` 換掉一個狀態機
- **不帶步數,所以字串反而少兩句**

**復原:離開就清,被殺才問**
- 明確離開一律清空,所以**磁碟上一份 dirty 的草稿只可能是行程死掉留下的** —— 檔案本身就是證據,不需要旗標
- 問的時機是工具列**第一次真的出現在螢幕上**,不是服務啟動時:沒人看得到的問句會被下一個動作代答
- **先還原再問。** 還原是安全的方向,步驟已經在記憶體裡,沒答等於沒損失

**首頁**
- **讀取移到列上,不在 `⋮` 裡。** 原本點片段列什麼都不做、點流程列開編排 —— 主要動作埋在選單,而最直覺的手勢一邊是空的、一邊是另一件事
- **讀取之後關掉主畫面**並打開工具列。服務沒啟用時不關,那時你要的是權限卡
- **`[直接開始(不讀檔)]`** —— 兩個清單都答不了最常見的開場,而讓清單當進入模式的路,破洞剛好開在第一次使用的位置
- 流程列的 `⋮` 多一個**編排**;新建流程改成建完就載入並開編排,返回直接回目標 app

### Fixed — flow mode 的 `⊕ 新建流程` 一直是死的

`openWorkspaceDialog` 的守衛寫成「除了 LOAD 以外,工作區空的就擋掉」,那是在「開新片段」還是其中一個模式的時代寫的。而 **flow mode 的工作區永遠是空的**,所以工具列上唯一能建流程的按鍵一路回「沒有東西可以存」,從來沒開過對話框。改成正面列出規則適用於誰(只有 SAVE),加第五個模式時不會再錯同一次。

### Added — M3 流程層

**流程 = 片段的集合,而它跟步驟層是同一個形狀往上長一層。** 這個一致性是整段設計的軸:容器裝成員,每個成員有前置延遲和重複次數,容器自己有迴圈次數 —— 步驟層與流程層完全相同。設計理由寫進 [SPEC §5.3 / §10.4 / §10.5](docs/SPEC.md)。

**資料層:四個型別收成一個**
- **`WaitNode` / `GlobalNode` / `AwaitTextNode` 拿掉。** 它們是下一層概念的複製品 —— `WaitNode(ms)` 跟 `PauseStep(ms)` 是同一個欄位,連顯示字串都共用 `step_wait`;`GlobalNode` 是功能較少的 `GlobalStep`。**node 就是步驟**,所以流程的成員只有片段
- **`ClipNode` 補上 `delayBefore` / `repeat` / `repeatIntervalMs`** —— 跟步驟層同樣的三個旋鈕。要在片段之間等就用 `delayBefore`,要按返回就在前一個片段尾端放 `GlobalStep`
- **預留的 `pauseAfter` 旗標不必做了** —— `delayBefore` 更一般化地解掉同一個問題(是長度而不是布林)
- **`Flow` 去掉 `startDelayMs` / `speed`** —— 跟 `Settings` 重複,兩個來源同一個數字遲早不一致
- 條件等待(M4)回到**步驟層**,不是節點:等文字出現在片段中間比只能放在片段之間有用得多

**執行:沒有第二個執行器**
- **流程展開成一條步驟清單交給既有的 `Player`**(`FlowPlan`)。所以它直接繼承每圈間隔、碰到暫停、單步重複、從第 N 步開始 —— 寫 `FlowRunner` 等於把那四件事再實作一次,然後兩邊開始漂
- **展開時逐 clip 各自做座標縮放。** 每個 `Clip` 有自己的 `screen` 而 `Player` 只吃一個,不這樣做,混了兩個錄製來源的流程會整條用其中一個的比例播出去 —— 而且是靜默的。這是既有的線性縮放套用在每個 clip 上,不是換機重算(那屬於匯出時)
- **展開會換掉步驟 id。** 一個片段重複五次會讓同一個 id 在清單裡出現五次,而畫面上每個標記都靠 id;今天不會壞,但那是留給下一個假設 id 唯一的人的陷阱
- **進度顯示 `片段 2/5 步驟 10/30`** —— 流程壞在哪個片段是最想知道的事。**只有一個片段時不顯示 `1/1`**,所以一條單片段流程讀起來跟直接播那個片段完全一樣
- **執行前遇到不存在的片段就拒絕開始**,不跳過那一格照跑 —— 少跑一格的流程結果是錯的但看起來是成功的

**flow mode 是第四個按鍵組,而且很薄(11 顆)**
- **clip mode 的對象是 node,flow mode 的對象是 clip** —— 差一層,所以功能量差很多。不能編 node、不能存 node,所以那一整排(拖曳、時長、重錄、插入、複製、隔離、列表、設定頁)一顆都不出現
- **沒有存檔鍵。** 編排畫面直接改的就是存起來的那條流程,所以「存流程」這個動作不存在
- `⊕` `📂` `−` 在兩個模式是不同的名詞(片段 / 流程),不另做圖示 —— 名詞跟著模式走。分辨依據是 **`▶` 在 flow mode 是另一個 icon**

**流程與工作區互斥**
- 同一時間只有一邊是滿的,所以 `▶` 只有一個意思,不需要「當前目標」這個概念
- **讀流程 → 清工作區,先警告**(可能弄丟沒存的錄製);**錄製 / 讀片段 → 卸流程,不問**(零損失,流程本來就存著)
- **錄製是離開 flow mode 的路** —— 擋著不讓錄很煩,而卸掉流程沒有代價
- 「兩邊都留著加一顆切換鍵」放棄了:切到空的那邊 `▶` 按下去什麼都不會發生,那顆鍵有一半的位置是死的

**畫面**
- 首頁多流程區塊(用掉了 `home_tab_flows` / `home_no_flows`),已讀取的那條有標記,每一列可刪
- **首頁有「新建流程」** —— 這是唯一能做出**第一條**流程的地方(見下)。它刻意**不讀取**新建的那條:在 app 裡建流程不該把你正在錄的東西清掉
- 編排畫面:片段列表、加入片段、上下移、移除、每列一顆設定鍵進去改三個旋鈕(不展開那一列 —— 三個滑桿放在每一列上會蓋掉這個列表要表達的順序)
- **`ClipDetailScreen` 決定不做** —— 匯出暫緩,改名與刪除本來就在首頁選單裡
- **刪片段會從所有流程移除那一列**,不再留「片段已刪除」佔位。確認框先說有幾條流程在用

**一個做完才發現的洞:flow mode 根本進不去**
- `⊕ 新建流程` 和 `📂 讀取流程` 都只掛在 flow mode 上,而 flow mode 要有流程被讀取才會進入。零流程的狀態下:首頁只顯示「還沒有流程」、工具列是 clip mode —— **第一條流程永遠建不出來,也永遠切不進去**
- 修法順便讓設計更乾淨:**`📂` 變成一顆、不分模式、一個對話框同時列出流程與片段。** 讀取是一個動作一個意思,而同一時間只有一邊是滿的 —— **你挑了什麼就決定了模式**。原本兩顆讀取鍵在同一個模式下還得共用圖示,那正是把它們做成模式互斥要避免的歧義
- 「新建流程」加進首頁的流程區塊
- **首頁的流程列補上 `⋮` 選單:讀取 / 改名 / 刪除** —— 跟片段列一模一樣,而「流程跟片段一樣操作」要有意義就得是這樣。**讀取原本整個不在** :點那一列開的是編排畫面,那是完全不同的事,所以從 app 裡根本沒有辦法進 flow mode
- **順帶修掉:首頁讀取片段沒有卸掉流程。** 只有 overlay 那條路做了,所以從 app 讀片段會留在 flow mode 裡 —— 兩邊互斥的規則破在這裡

**兩個做的時候抓到的 bug**(用腳本從 `ToolbarView` 反推四個模式的可見集合才發現的)
- **`✎` 在編輯模式消失了** —— 我把它從 `!recording` 改成 `idle`,於是編輯模式在工具列上**沒有任何出口**
- **三顆流程鍵沒進 `scrollingButtons`** —— 宣告了、接了事件、設了可見性,但不在按鍵欄裡,所以永遠不會出現

### Fixed — 播放中碰到螢幕會報「下發失敗」然後繼續跑(實機回饋)

**先講清楚:注入的手勢不會跟真實觸控疊加。** 框架的 `MotionEventInjector` 一收到真實輸入就取消所有在飛的注入手勢 —— 真實輸入優先,這是平台行為,app 改不了。所以手指碰下去的確會取消那一步。

- **但真正的問題不是那個訊息,是那一步被取消之後播放器記個 log 就跑下一步。** 後面每一步都是對著「沒發生那一步」的畫面在執行,而訊息只是把這件事蓋掉
- **改成:播放中手勢被中途取消 → 暫停,並在面板上說明是第幾步被觸控中斷。** 不再 toast「系統取消了手勢」,那句話讀起來像系統或目標 app 有問題,而實際上是你的手
- **被中斷的那一步不會自動重做。** 中途取消的滑動可能已經送出一半,重送不安全 —— 這跟 `GestureDispatcher` 不重試中途取消是同一個理由。提示會寫出步號,要重做就用「從第 N 步開始」
- **不列入連續失敗計數。** 那個計數存在的目的是分辨「偶爾一次被取消」與「每次都被取消」,那是關於系統的問題,不是關於使用者的手
- **錄製時的逐手勢補發仍然照舊 toast** —— 那時候沒有播放可以暫停
- **SPEC §14 第 8 條原本寫「播放中偵測不到使用者碰螢幕,所以沒有碰到就暫停的保護」,那是錯的。** 偵測不是直接的,但手勢被取消本身就是訊號,而在正常裝置上其他成因都是本 app 控制得住的(不同時派送兩個手勢、重播期間刻意不動畫布),缺少注入器那一種在時序上先被分出去了

### Added — 整份腳本迴圈之間的間隔
- **設定 → 執行多一項「每圈之間的間隔」**(預設 500ms,範圍 0–30s)。原本兩圈之間只有第 1 步的前置延遲,而那是錄製時填的預設值(第一步沒有前一步可以量),所以兩圈幾乎是貼著跑的
- **跟 §9.9 單步重複的間隔是同一條道理,只是上一層。** 第 1 步的 `delayBefore` 是「第 1 步之前」的引子,不是「兩圈之間」;兩者是不同的量,用同一個欄位表達其中一個就會壞
- **也不用 `startDelayMs` 代替** —— 那個倒數存在的理由是讓你切到目標 app,一次執行只發生一次。拿它當每圈的間隔,一百圈就是一百次三秒
- **只在圈與圈之間,最後一圈之後沒有。** delay 放在迴圈本體最上面、跳過第一圈,所以這件事是免費的
- **間隔中間也過暫停檢查點**,暫停與停止在間隔裡就生效
- **無限迴圈時最重要** —— 沒有它腳本會不停連續打下去
- 播放面板在間隔中只顯示圈數(`↻ 2 / 3`)不顯示步數,因為那時候沒有任何一步在跑 —— 印 `0 / 100` 會讀成計數器壞了

### Added — 單一步驟可以原地重複 N 次
- **手勢與全域動作多了「重複」與「重複間隔」**,設定頁上兩列。十下點擊是一步,不是複製九次得到的十步
- **它是少數會讓腳本變短的編輯功能。** `47 ▸ 輕觸 540,1200 ×10` 是一行不是十行,而變短是一百步腳本唯一真正有幫助的事。它也通過了 §9.11 那個判準(不需要讀得懂腳本 —— 設定的那一刻你就知道自己要它按十下),而重排、絕對位置搬移、型別互轉都沒通過
- **`repeatIntervalMs` 跟 `delayBefore` 刻意分開。** 一度考慮讓 `delayBefore` 在重複時每次都套,但那會讓同一個欄位在 `repeat = 1` 時是一個意思、大於 1 時是另一個 —— 意義取決於另一個欄位。而且「上一步之後等 2 秒,然後以 200ms 連點十下」會表達不出來。`delayBefore` 是**兩個不同動作**之間的節奏(錄製量出來的),間隔是**同一個動作**每次之間的距離
- **暫停不能重複。** 秒數等待 ×10 是一個更長的等待,手動暫停 ×10 是停十次 —— 所以 `PauseStep` **不實作 `RepeatableStep`**,而不是實作了再忽略
- **`GlobalStep` 可以重複**,「按返回三次」是最好的用例之一 —— 不過它目前還沒有任何建立入口(見 ROADMAP)
- **調到 2 以上會自動填入一個可用的間隔**(全域預設 gap)。間隔 0 會逐次連發,下層 app 會把十下讀成一次多重點擊或直接丟掉。寫進步驟而不是播放時代入,否則面板顯示「0 ms」卻不是那樣跑
- **每一次之間都過暫停檢查點**,所以暫停與停止在重複中間有效,不是只在步驟之間
- 播放面板顯示 `(3/10)`,只在真的有重複時出現 —— 同一個編號停留好幾秒不顯示會讀成卡住
- 上限 999;中途失敗沿用既有規則(回報並繼續),不為這個功能發明新的失敗語意
- 兩個欄位都有預設值(`1` 與 `0`),**舊的片段與草稿照樣載入且行為不變**

### Changed — 插入不再有方向,方向搬到移動鍵上
- **設定頁的「往前插」拿掉,換成 `«` `»` 前後移一格。** 拿掉的理由不是它不好用,是**四顆插入鍵裡只有一顆帶方向,那一顆就會讀成例外而不是規則** —— 而且它跟主選單的 `＋` 方向相反。移動鍵不可能沒有方向,所以方向搬到那裡,插入就變成一條沒有例外的規則
- 要放到第 1 步前面就是**插完往前移一格**。`Workspace.insertBefore` 隨之變成死程式,拿掉;`Capture.InsertBefore` 也拿掉
- **移動就是整串重排,`delayBefore` 跟著步驟走。** 一個步驟就是一個東西,搬或複製都不可能只搬一部分 —— 延遲屬於步驟而不屬於它坐的那一格,所以搬動不能偷偷改掉它的時間。整體節奏會變,但那就是搬動的意思,而延遲本來就能單獨改
- 移動會 snapshot,所以 `↩` 復原得回來
- **只做 ±1。** 「移到第 N 步」與長按指定絕對位置討論過並砍掉,「錄反了換順序」也不是留下它的理由 —— 兩者都預設你讀得懂腳本,而那是做不到的(見下)
- **版面:兩顆移動鍵擺在最左與最右**,各自在它指的那一邊;中間留給複製與刪除,彈性空白把那兩顆拉到這排能做到的最遠。用雙箭頭 icon 而不是文字 —— 標頭的單箭頭移動的是你,這裡移動的是這一步,要一眼分得出來,而翻譯後的文字長度各語系差很多會把這排推歪。到端點變灰不隱藏,否則整排會橫移

### Documented — 編輯能力的天花板是讀不讀得懂
- **新增 SPEC §9.11,它不描述功能,是用來砍功能的判準。** 列表上是 `47 ▸ 輕觸 540, 1200` —— 座標不是意義,它沒告訴你那一下按到了什麼。任何預設「你知道每一步在做什麼」的功能實際上都做不到:重排腳本、絕對位置搬移、步驟型別互轉,全部倒在這一條上
- 可行的編輯是**小範圍修正**:播放停在第 47 步 → 改座標或時間 → 重跑。這是要服務好的情境
- **抬高天花板的方向不是加編輯功能,是讓步驟讀得懂** —— 錄製時記下觸點底下節點的文字,列表變成 `47 ▸ 輕觸「確認」`。列進 ROADMAP 待議,連同四個已知的坑(自繪畫面沒節點、熱路徑成本、觸點與解析的時間差、空標籤的退回顯示)。**只記錄不做**

### Added — 設定頁也能複製與刪除這一步
- **設定頁多一顆「複製」**,跟主選單的 `⧉` 同一個行為:複製、插在它之後、選到新的那一個。在第 5 步按下去就多一個第 6 步,內容跟 5 相同,而且面板直接落在 6 上
- **設定頁多一顆「刪除」**。它一開始被擋下來,是因為「按完面板要去哪」沒有答案 —— 定下來之後那個顧慮就沒了
- **刪除的選取規則改成一律往前一格**(原本是接手缺口的那一步,也就是往後)。刪第 100 步 → 落在 99,這正是隔離那條規則(§9.2)在講的事;同一條規則在腳本中間也成立 —— 刪第 47 步 → 落在 46,你留在缺口後面而不是站在缺口上。第一步例外,它前面沒有東西,所以補上來的那一步就是你剩下的。刪光了就離開編輯模式
- **三顆都跟主選單共用同一段程式**(`duplicateSelected()` / `deleteSelected()`)。兩個入口一種行為沒問題,兩份實作才會慢慢不一致
- **刪除排在最左邊、跟產生步驟的那兩顆隔開** —— 擺在一起就是伸手要按複製卻按到刪除

### Fixed — `↩` 的名字停在被換掉的舊實作上
- **「復原上一步」改成「復原」**,toast 從「已移除最後一步」改成「已復原」。M1 的實作是 `dropLast(1)`,那個名字當時是準確的;實作換成狀態快照堆疊之後名字沒跟上,於是它讀起來像「刪除第 100 步」—— 跟旁邊的 `−`「刪除選取」擺在一起,就變成兩顆看起來都在刪東西、只差刪哪一個的鍵
- **行為沒動。** `↩` 復原的是上一次改動,不管那是拖曳、插入、改時長還是刪除 —— 這正是它跟 `−` 不重疊的地方:不小心把第 47 步拖走 200px,`−` 只會把 47 刪掉,`↩` 才會把它放回原位

### Added — 重錄單獨一步
- **參數卡多一顆「重錄」**(只對手勢步驟出現)。按下去,你下一個手勢就取代這一步。一個點擊落錯位置,不該讓整個 100 步的腳本重錄 —— 那也是當初要做流程層的理由
- **保留 `delayBefore`**。那是這一步跟前一步之間的節奏,重做動作不是丟掉它的理由,而且它本來就能單獨編輯
- **不做逐手勢補發。** 補發的用意是讓下層 app 跟著錄製往前走;編輯時沒有那個同步關係,派送只是無故去戳目標 app
- 不倒數,理由跟恢復錄製一樣。誤觸的代價由 `↩` 承擔 —— 現在它是真的復原了

### Added — 用編號找到步驟:可捲可點的步驟清單
- **編輯模式多一顆 `☰`**,開出一個可捲動、可點選的步驟清單 window。100 步的腳本用標記選取是行不通的:標記互相重疊,而**播放回報的是編號**(`⤳ 47 / 100`)—— 原本沒有任何辦法把那個數字換回一個標記
- 清單裡有**跳到第 N 步**、上一步、下一步。跳步複用等待秒數那個數字鍵盤(`NumberPadView` 改成標題、單位與處理函式都由呼叫端給,所以一個 pad 服務兩種問題)
- 做成獨立 window:原本畫在畫布上的清單只顯示塞得下的尾端、不能捲,而畫布裡的可捲區域會跟拖曳搶垂直手勢
- 選取是**共用狀態**,所以按 `☰` 收起清單就回到用標記拖曳,選的還是同一步。兩種入口不需要各自一套動作
- 清單只在編輯模式出現,離開編輯會自動收起
- **清單可拖曳**(抓標題),參數卡預設移到螢幕頂端。兩者原本相距不到一成螢幕高,實機上直接疊在一起、擋住要看的東西。拖曳把手刻意不放在清單本體 —— 那裡的垂直手勢要留給捲動,跟工具列把把手放在 ScrollView 外面是同一個理由
- 行只在文字真的變了才重建 —— 這個 view 跟其他面板走同一個刷新流程(播放時每秒數次),每次重建 100 個 TextView 正是這個專案已經吃過的虧

### Added — 從第 N 步開始播放
- **待機工具列多一顆 `▶|`**:跳數字鍵盤問從第幾步開始。找到第 47 步壞掉、改好之後,確認不該需要重跑 1–46
- **放在播放鍵旁邊,不放在編輯的參數卡裡。** 一開始做在參數卡上,但那是錯的分類 —— 決定執行從哪裡開始是**執行**的性質,跟編輯無關。實機回饋直接指出這點
- 用當下的選取預填,而選取離開編輯模式後還留著,所以剛改完第 47 步數字已經在那裡
- **只有第 1 圈從第 N 步起跑,第 2 圈起回到第 1 步。** 「從第 47 步開始跑 3 圈」幾乎不可能是指每一圈都跳過 1–46
- **起始那一步的前置延遲會跳過** —— 它是相對前一步量出來的,而前一步沒有執行。開始前的倒數已經涵蓋那個空檔

### Added — 編輯模式可以插入了
- **`⏸+` 與 `⏱+` 在編輯模式也出現**,插在**選取之前**。原本它們只在錄製中出現,所以錄完之後**沒有任何辦法在第 1 步和第 2 步之間插入等待** —— 而那正是「等待 N 秒」這個功能最初要解決的情境
- 編輯模式的按鍵從 7 顆變 11 顆(多了 `⏸+` `⏱+` `↩` `☰`)

### Added — 暫停點的備註終於能填了
- **參數卡多一列「備註」**:選取暫停點 → 按「備註」→ 跳出輸入畫面。`PauseStep.note` 這個欄位從 M1 就存在,步驟清單與暫停提示都早就會顯示它,但**一直沒有任何地方能輸入**
- 走 `WorkspaceDialogActivity` 的第四種模式。自由文字需要輸入法,而懸浮視窗一律 `FLAG_NOT_FOCUSABLE`,所以不能像等待秒數那樣用自繪鍵盤 —— 那個只需要數字
- **插入時仍然不問**,維持 `⏸+` 一按就好(SPEC §10.1)。按下去的下一刻使用者要的是目標 app 在眼前讓他動手,那時候彈輸入框只會擋路
- **編輯模式插入時會自動選取剛建立的那一步**,所以參數卡直接開在它上面。錄製中刻意不選取 —— `insertAfter` 拿選取當定位點,而錄製的步驟必須一直附加在尾端
- 留空是有效的,不是取消:提示會退回預設那句,而想清掉舊備註也不必刪掉整個步驟
- 時間性的等待也能填備註(「等 5 秒讓動畫跑完」),不限手動暫停

### Added — 插入「等待 N 秒」,與暫停點合為同一型別
- **工具列多一顆 `⏱+`(錄製中)**:按下去跳出數字鍵盤輸入秒數,插入一個會自己繼續的等待。原本「第 1 步和第 2 步之間時間沒抓好」只能事後去參數卡加前置延遲,而那是 100ms 一格,加 5 秒要按 50 下
- **`WaitStep` 折進 `PauseStep`。** 兩者都是「在這裡停一下」,差別只在誰來解除:計時器還是手指。`ms = 0` 等使用者,`> 0` 等那麼久。SPEC §1.4 早就規定全 app 只有一個暫停機制,多一個型別本來就與它衝突,而且會讓標記、清單文字、執行分支各做兩份
- **既有片段不受影響。** `@SerialName("pause")` 不變,新的 `ms` 欄位有預設值,所以合併前存的片段照舊載入。`WaitStep` 從來沒有任何 UI 能建立它,所以沒有任何存檔含有它 —— 零遷移風險
- **有秒數的那種不會進 `PAUSED`**,只是 `RUNNING` 裡的一段延遲,進度照跑。`PAUSED` 仍然只代表「有事情需要人」
- **UI 上仍是兩顆按鍵。** 共用一顆的話,按下去得先問「幾秒還是等我」,那等於在一個刻意零對話框的流程(§10.1)前面加一個對話框。資料合一,入口分開
- **`⏱+` 不停止錄製**(`⏸+` 會)。停止錄製的用意是讓你動手做事,而等待不需要你做任何事
- 數字鍵盤是**畫在 overlay 上的按鈕,不是 Activity**。懸浮視窗一律 `FLAG_NOT_FOCUSABLE` 所以不能有輸入框,但數字不需要輸入法 —— 十個數字加退格與確認都只是 view。這樣錄製中插入等待**不會把目標 app 推到背景**
- 步驟清單與參數卡都會分辨兩者:「⏸ 暫停點」對「⏱ 等待 3 秒」

### Removed — 錄製時的警示區整個取消
- **斜線警示區與它的文字說明全部移除。** 那塊區域使用者本來就碰不到,沒有什麼行為需要被勸阻,而斜線與文字都在錄製時佔掉螢幕
- **規格原本要求的「編輯時落在警示區內的標記轉黃」也一併取消**,不再列為待實作的缺口
- 連帶移除 `CanvasView.blockedAreas`、`MarkerPainter.drawBlockedArea()`、`TapFlowService.syncBlockedAreas()`、`warn_hatch` 顏色與 `warn_toolbar_area` 兩個語系的字串
- 這個限制本身只在 README 說明。而工具列與播放面板不再同時出現之後,死區本身就縮小了

### Changed — 工具列與播放面板不再同時出現
- **播放 / 倒數 / 暫停時,工具列整個不顯示。** 原本工具列的 `▶`(暫停)、`⊙`(停止)跟播放面板的 ⏸ / ■ 是同一組功能各做一份。而且每多一個懸浮視窗就多一塊死區 —— 播放時落在上面的那一步會被吃掉(README 限制 2),所以這不只是視覺重複
- **錄製時不再顯示播放面板。** 它在錄製中只有一顆 ■(跟工具列的 `⊙` 重複)加上「錄製中 N 步」。步數不需要補到別處:標記本來就帶序號,連 `隱藏` 密度都會留最近 1 個並淡出
- 由此得到一個不變式:**錄製情境用工具列(展開 → 球),播放情境用播放面板,兩者永不同時出現**
- **播放暫停不再收成球。** 工具列本來就不在,鍵盤區是空的。錄製暫停(`⏸+`)維持原樣收成球 —— 那條路徑的工具列還在,不讓位就會壓住鍵盤的 Q/A/Z 那排,而那是 HANDOFF 上唯一標實機驗過的流程
- 連帶移除 `BallIntent.RESUME_PLAYBACK`、`Player.onPausedChanged` 與 `restoreFromBall()`。那個 callback 只是為了收球而存在,而 `EngineState.mode` 已經是服務在觀察的狀態;錄製那條的復原則本來就由 `startRecording()` 負責

### Changed — 每個模式各自一組工具列按鍵
- **待機 10 顆、錄製 5 顆、編輯 7 顆**,取代原本不分模式都 12 顆、其中 5 顆變灰。SPEC §9 早就立過「不把可以直接不顯示的按鍵變灰」這條原則,但只套用在編輯模式
- `⏸+` 與 `↩` 移出待機組。`⏸+` 在待機只能附加在尾端 —— 待機不可能有選取,而「插在選取後面」才是它有價值的用法;`↩` 當初的存在理由是「完整的選取刪除排在 M2」,M2 已經做完
- `⚙` 與 `✕` 移出錄製組。前者唯一會想當場改的是逐手勢補發,而它正常就開著;後者會關掉整個工具列,而那是停止錄製的唯一入口
- **順帶修掉一個 bug:`💾` 在錄製中原本是可以按的。** `📂` `⊕` `✎` `✕` `▶` 都有 `!recording` 條件,只有它漏了。按下去會開一個搶焦點的 Activity,把目標 app 推到背景,而畫布還在攔截。SPEC §3.1 的表一直寫著它在錄製中該是「—」
- 工具列的隱藏**由 mode 推導,不存成第三種 `ToolbarForm`**。存起來的「隱藏」正是 `✕` 那次「工具列再也叫不回來」的成因;推導出來的不可能卡住

### Fixed — 暫停點刪不掉
- **暫停點現在畫成標記,可以點選、可以刪除。** `buildMarkers()` 原本跳過所有非手勢步驟,所以暫停點沒有標記 → 編輯模式選不到 → `−` 刪不掉。**`↩ 復原上一步` 是當時唯一能移除它的手段**,而 `⏸+` 一按就停止錄製把人丟進待機 —— 誤插一個就只能 `⊕ 開新的` 整個清空
- 節點畫在序號連接虛線上,**不畫準心** —— 準心的意思是「就是這個像素」,而暫停點沒有像素
- **可點選但不可拖曳。** 它的錨點是從前後鄰居推導的,拖了不會改變任何存下來的值,下次重繪就彈回去
- 錨點推導處理五種情形:夾在兩個手勢之間(落在連線上)、連續多個(沿線散開,不重疊)、在最後一個之後(沿來向延伸)、在第一個之前(逆著去向退開)、**完全沒有手勢步驟**(沒有東西可參照,退回畫面中央直排)
- `Marker.kind` 從資料層的 `GestureKind` 換成 overlay 層的 `MarkerKind`,多了 `PAUSE` 與 `WAIT`。繪製程式碼因此不需要認得資料層的 sealed step 型別
- `WaitStep` 一併做好。它還無法建立,但第一個插入它的 UI 不用再回頭補這一塊

### Documented — 文件與程式碼重新對齊
- **規格說「只用三個 window」,實際有五個。** 快捷設定面板與參數卡都是自己的 window(`host.add`),而 SPEC §3.3 還寫著參數卡「畫在畫布上,不另開 window」—— 跟同一份文件 §9 的「參數卡是獨立的 window」直接打架。SPEC §3 改成如實描述五個,並說明三個常駐、兩個按需掛上
- 工具列按鍵數三處數字全錯(「14 排」「11 顆」「10 排以內 / 13 排」)。實際是宣告 14 顆、編輯模式外顯示 12 顆、編輯模式 7 顆
- SPEC 的檔案樹還停在 `D:\touch\`,少列七個實際存在的檔案(`WorkspaceDialogActivity` / `QuickSettingsView` / `Diag` / `EngineState` / `Timing` / `ServiceStatus` / `DiagnosticsScreen`),`ParamCard.kt` 的檔名也不對。M3 / M4 尚未存在的檔案現在明確標記出來
- SPEC 缺 §10.3(從 10.2 跳到 10.4),而 §3.1 有一處正指著它。交叉引用改指 §10.2(2),10.4 遞補為 10.3

### 尚待實機驗收
M1 的三個風險項目還沒在實機上驗過,見 [docs/SPEC.md](docs/SPEC.md) 第十五節:
逐手勢補發的手感、`FLAG_NOT_TOUCHABLE` 切換是否可靠、暫停時鍵盤是否真的按得到。
